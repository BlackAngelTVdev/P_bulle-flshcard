@component('components/main', { title: 'Decks | Flash Dam\'s', css: 'createdeck' })
<h3>
    Ajouter un deck
</h3>
<form action="{{ route('decks.store') }}" method="POST">
    {{ csrfField() }}
    <div class="field">
        <label for="name">Nom du deck</label>
        <input type="text" id="name" name="name" value="{{ old('name') || '' }}" />
        @inputError('name')
        <p class="text-danger">{{ $messages.join(', ') }}</p>
        @end

        <label for="description">Description</label>
        <input type="text" id="description" name="description" value="{{ old('description') || '' }}" />
        @inputError('description')
        <p class="text-danger">{{ $messages.join(', ') }}</p>
        @end
    </div>
    <button type="submit">Enregistrer</button>
    <a href="{{ route('decks.index')  }}" class="btn-abandon">Abandonner</a>
</form>
@end