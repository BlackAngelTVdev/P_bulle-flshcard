<header>
  <div class="container-header">
    <div class="titre-header logo-text">
      <a href="{{ route('decks.index') }}" aria-label="Retour a l'accueil">
        <h1>
          <span class="logo-flash">Flash</span>
          <span class="logo-dams">Dam's</span>
        </h1>
      </a>
    </div>

    <input type="checkbox" id="menu-toggle" class="menu-toggle" />
    <label for="menu-toggle" class="burger-label">
      <span></span>
      <span></span>
      <span></span>
    </label>

    <nav>
      <div class="nav-links">
        <a href="{{ route('decks.index') }}">Accueil</a>
        <a href="{{ route('decks.mine') }}">Voir mes decks</a>
        <a href="{{ route('decks.create') }}">Ajouter un deck</a>
        @if(auth.user && auth.user.isAdmin)
          <a class="admin-link" href="{{ route('admin.index') }}">Administration</a>
        @end
      </div>
      <div class="user-info">

        <a href="{{ route('profile.edit') }}">          <span class="username">{{ auth.user.username }}</span></a>
        <form action="{{ route('auth.logout') }}" method="POST" class="logout-form">
          {{ csrfField() }}
                    <button type="submit" class="logout-button">Se déconnecter</button>
        </form>
      </div>
    </nav>
  </div>
</header>
